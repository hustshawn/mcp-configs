{
  "mcpServers": {
    "amap-maps": {
      "command": "uvx",
      "args": [
        "amap-mcp-server"
      ],
      "env": {
        "AMAP_MAPS_API_KEY": "${AMAP_MAPS_API_KEY}"
      }
    }
  }
}